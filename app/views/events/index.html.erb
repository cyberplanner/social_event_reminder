<h1>FamilyConnect</h1>
<%= link_to "New Event", new_event_path %>

<% if @events.any? %>
  <% @events.each do |event| %>
    <h2><%= event.title %></h2>
    <p><%= event.date %></p>
  <% end %>
<% end %>
