<div class="form-wrapper">
  <h2 class="text-primary">My Family</h2>
  <% @users.each do |user| %>
  <h2 class="text"><%= link_to user.username, user, class: "text text-info" %></h2>
  <hr>

  <% end %>
</div>
